[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Add markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration tests
    unit: unit tests

# Configure test output
addopts = -v --cov=utils --cov-report=term-missing --cov-report=xml:coverage.xml
